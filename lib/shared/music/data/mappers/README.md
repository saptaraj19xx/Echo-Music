Firestore mappers for the Music read layer.

These classes convert Firestore document maps (Map<String, dynamic>) into domain entities.

Rules:
- No Firebase imports here.
- No Firestore field mapping inside repositories.

