/// Centralized animation durations for Echo.
class AppDurations {
  AppDurations._();

  /// Total sprint-3 splash duration (~2s).
  static const Duration splashTotal = Duration(milliseconds: 2000);

  static const Duration splashBackground = Duration(milliseconds: 2000);
  static const Duration splashLogoIn = Duration(milliseconds: 650);
  static const Duration splashTaglineIn = Duration(milliseconds: 500);
  static const Duration splashPulse = Duration(milliseconds: 1400);
}


